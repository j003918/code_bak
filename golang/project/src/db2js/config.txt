mysql_user{
	select * from user where USER="#user#"
}

oci8_dept{
select 
--DEPT_CODE ,
NAME ,
USER_NAME
from COMM.STAFF_DICT
}

hisuser{
select 
user_name,name,
CREATE_DATE
from COMM.STAFF_DICT
order by CREATE_DATE
}

job{
select 
status
from COMM.STAFF_DICT
}