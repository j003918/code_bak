fee{
	select name,id 
	from reimbursement_fee_type 
	where 1=1
	and id >=#sid#
	and id <=#eid#
}

dept{
	select * from v_jhf_dept
}

user{
	select a.username,a.userid,a.fullname,a.depid,b.depname 
	from app_user a 
	left join department b on a.depid=b.depid
}

